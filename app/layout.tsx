import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
    title: 'Yantras - Building the Operating Systems for Modern Businesses',
    description: 'One platform, many products. Scalable, modular, future-ready business software. Complete ERP modules, automation tools, and integrated systems.',
}

export default function RootLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <html lang="en">
            <body className={inter.className}>{children}</body>
        </html>
    )
}
